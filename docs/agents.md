sed: --: No such file or directory
# Agents

The orchestrator is code, not an autonomous agent. Discovery, verification, materiality, synthesis and QA are bounded Agents SDK calls with Zod outputs. Future specialists use the same contract: judgement only, no publication side effects. Model IDs are environment-configurable; agent runs store response/trace metadata and usage where available.
