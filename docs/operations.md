sed: --: No such file or directory
# Operations

Runs progress through the enumerated state machine. Workflow steps retry transient failures independently; fatal evidence or QA errors stop the run. Manual and scheduled triggers use the same workflow. Approval resumes `report-approval:<report_run_id>`. Monitor workflow runs, Supabase logs, advisor results, OpenAI traces, report cost and stale awaiting-approval runs.
