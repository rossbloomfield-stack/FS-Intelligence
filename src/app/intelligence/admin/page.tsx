sed: --: No such file or directory
import Link from"next/link";import{SectionPage}from"@/components/intelligence/section-page";import{fixtureMode}from"@/config/env";import{requireAdmin}from"@/lib/supabase/auth";
export const dynamic="force-dynamic";export default async function Page(){if(!fixtureMode)await requireAdmin();return <SectionPage eyebrow="ADMIN" title="Intelligence operations" description="Generate, monitor, review and approve weekly intelligence runs."><div className="grid gap-5 lg:grid-cols-3"><AdminCard title="Generate report" body="Start the same durable workflow used by the weekly schedule." action="Generate report now"/><AdminCard title="Run progress" body="Agent and workflow status will update from persistent run state." action="Open latest run" href="/intelligence/admin/runs/fixture"/><AdminCard title="Draft review" body="Publication remains blocked until QA passes and an administrator approves." action="Review draft"/></div></SectionPage>}
function AdminCard({title,body,action,href}:{title:string;body:string;action:string;href?:string}){const cls="mt-5 inline-flex rounded-lg bg-[var(--purple)] px-4 py-2 text-sm font-semibold text-white";return <article className="rounded-xl border border-[var(--line)] p-5"><h2 className="text-lg font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{body}</p>{href?<Link className={cls} href={href}>{action}</Link>:<button className={cls}>{action}</button>}</article>}
