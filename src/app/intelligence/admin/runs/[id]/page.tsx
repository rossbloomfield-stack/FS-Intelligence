import{SectionPage}from"@/components/intelligence/section-page";const stages=["Discovery","Verification","Materiality","Pattern analysis","Synthesis","QA","Approval","Publication"];export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <SectionPage eyebrow="RUN PROGRESS" title={`Report run ${id}`} description="Durable stages can be inspected and retried independently."><ol className="divide-y divide-[var(--line)]">{stages.map((s,i)=><li className="flex items-center justify-between py-4" key={s}><span><strong className="mr-3 text-[var(--purple)]">{String(i+1).padStart(2,"0")}</strong>{s}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">Queued</span></li>)}</ol></SectionPage>}
