import Link from "next/link";import {SectionPage}from"@/components/intelligence/section-page";import{fixtureReport as r}from"@/lib/fixtures/report";import{fixtureMode}from"@/config/env";export const dynamic="force-dynamic";export default function Page(){return <SectionPage eyebrow="ARCHIVE" title="Weekly reports" description="Permanent, comparable weekly intelligence reports.">{fixtureMode?<Link className="flex items-center justify-between rounded-xl border border-[var(--line)] p-5" href={`/intelligence/reports/${r.slug}`}><span><strong>{r.createdAt}</strong><span className="mt-1 block text-sm text-[var(--muted)]">{r.headline}</span></span><span className="text-sm font-semibold text-[var(--purple)]">Open report</span></Link>:<p className="text-sm text-[var(--muted)]">No approved reports have been published.</p>}</SectionPage>}
