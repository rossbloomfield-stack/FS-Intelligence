sed: --: No such file or directory
"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/browser";
export default function Login(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const supabase=createClient();const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/api/auth/callback`}});setMessage(error?error.message:"Check your email for a secure sign-in link.");}return <div className="mx-auto max-w-md px-6 py-24"><div className="rounded-2xl border border-[var(--line)] bg-white p-8"><p className="text-xs font-bold tracking-[.16em] text-[var(--purple)]">ADMIN ACCESS</p><h1 className="mt-2 text-3xl font-semibold">Sign in</h1><p className="mt-3 text-sm text-[var(--muted)]">Use an approved administrator email address.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-[var(--line)] px-4 py-3"/></label><button className="w-full rounded-lg bg-[var(--purple)] px-4 py-3 font-semibold text-white">Send sign-in link</button></form>{message&&<p role="status" className="mt-4 text-sm">{message}</p>}</div></div>}
