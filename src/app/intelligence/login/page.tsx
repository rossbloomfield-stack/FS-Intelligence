"use client";
import { useState } from "react";
import { ShieldCheck } from "lucide-react";
import { createClient } from "@/lib/supabase/browser";
export default function Login(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const supabase=createClient();const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/api/auth/callback`}});setMessage(error?error.message:"Check your email for a secure sign-in link.");}return <main className="login-screen"><section className="login-proposition"><div><p className="login-brand">Irish Life <span>Market Intelligence</span></p><h1>Understand the market before it becomes obvious<span className="text-[var(--coral)]">.</span></h1><p>Continuously monitored hard and soft signals, structured for clarity, context and confidence.</p></div><ul><li>Evidence-linked answers</li><li>Structured market memory</li><li>Private executive workspace</li></ul></section><section className="login-action"><div className="login-card"><h2>Welcome back</h2><p>Sign in with your approved work account.</p><form onSubmit={submit}><label>Work email<input required autoComplete="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><button>Continue securely</button></form>{message&&<p role="status" className="login-status">{message}</p>}<p className="login-secure"><ShieldCheck size={18}/> Access is limited to approved users.</p></div></section></main>}
